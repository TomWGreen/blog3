Create the repo with GitHub desktop - blog3
Publish the repo - make public
Laragon - new laravel-skeleton
Copy the contents of the above folder into the newly created GitHub desktop folder 
- overwrite all files in destination directory

git add .
git commit -m "feat - Skeleton scaffolding"
Note - We are not performing a 'git tag' here, 
not because there would be nothing to see from a front end perspective, 
but because we do not have anything worth publishing yet

composer require laravel/breeze --dev
php artisan breeze:install
npm install
npm run dev

git add .
git commit -m "feat - Breeze install"

Update .env & config/database.php
Update DatabaseSeeder.php to create users
php artisan migrate
php artisan db:seed


Our app will default to the welcome page. Successful authentication will go to the dashboard.
Welcome blade to be altered to display list of posts.
Dashboard to be altered accordingly.

Left this out (routes/web.php) - dashboard to stay as is.
 Route::get('/', [PostController::class, 'home'])->middleware(['auth'])->name('home');

 php artisan make:model Post -mcf
 Copy contents of model, migration and factory
 php artisan migrate:fresh
 Update DatabaseSeeder.php to create posts (include user creation)
 php artisan db:seed
*** CONTROLLER *** php artisan make:controller PostController (insert contents)

Leave default authenticated redirection to dashboard as is (app/Providers/RouteServiceProvider.php) - 
public const HOME = '/dashboard';

*** MIGRATION *** php artisan make:migration create_posts_table
php artisan migrate

*** MODEL *** php artisan make:model Post (insert contents)
create post-guest.blade.php
NOTE - Update this with the rating later
Update PostController with welcome
Update web.php

git add .
git commit -m "feat - Guest welcome page"
git tag -a v1.0.0 -m "Version 1.0.0"

create edit/delete blade components
create post-auth.blade.php
Update PostController
Add routes to web.php

Create post_form.blade.php for creation/editing

Add link for creating post in navigation bar (layouts.navigation) - under Home
    <x-nav-link :href="route('post.form')" :active="request()->routeIs('post.form')">
        {{ __('Create Post') }}
    </x-nav-link>

*** FACTORY *** php artisan make:factory PostFactory --model=Post
Copy contents to PostFactory
Run seeders - php artisan db:seed

composer require --dev laravel/dusk
php artisan dusk:install

git add .
git commit -m "feat - CRUD blog"

Testing stuff

git add .
git commit -m "test - CRUD blog"
git tag -a v1.1.0 -m "Version 1.1.0"

Rating stuff
============
composer require  willvincent/laravel-rateable
php artisan vendor:publish --provider="willvincent\Rateable\RateableServiceProvider" --tag="migrations"
Left out??? Add to config/app.php         willvincent\Rateable\RateableServiceProvider::class,

Update Post model
    use Rateable;

php artisan migrate

php artisan make:seeder RatingsSeeder
Update seeder
php artisan db:seed

Update post-auth.blade.php & post-guest.blade.php with post rating (between title & content)
Update post-auth.blade.php with user post rating section



Nice to haves:
1) List posts by creation date descending
2) 404
3) Delete confirmation? Maybe